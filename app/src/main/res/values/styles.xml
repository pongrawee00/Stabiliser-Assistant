<resources>

    <!-- Base application theme. -->
    <style name="Theme.Light" parent="android:Theme.Light">
        <!-- Customize your theme here. -->
        android:textColor="@color/white_arctic"
    </style>

    <style name="Theme.Light.DialogWhenLarge" parent="android:Theme.DeviceDefault.Light.DialogWhenLarge">
        android:textColor="@color/white_arctic"
    </style>

</resources>
